@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
}

@model TheWebsite.Models.Utilisateur 

@*<div class="container p-2 my-2 border d-flex justify-content-between">
    <div class="py-3">@Model.Name @Model.LastName</div>
    <div class="p-2"><button type="button" class="btn btn-success" asp-action="@ViewBag.Action">@ViewBag.BtnText</button></div>
</div>*@

<div class="container p-2 my-2 border d-flex justify-content-between">
    <div class="py-3">Antoine Rioux</div>
    <div class="p-2"><button type="button" class="btn btn-success" asp-action="@ViewBag.Action">@ViewBag.BtnText</button></div>
</div>